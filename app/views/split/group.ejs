<!DOCTYPE html>
<html>
<head>
    <title>Group Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-primary shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold text-light">𝕤𝕡𝕝𝕚𝕥 𝕓𝕚𝕝𝕝𝕪</a>
            <div class="d-flex align-items-center">
                <a href="/logout" class="btn btn-outline-danger btn-sm">
                    <i class="bi bi-power">Logout</i>
                </a>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <!-- Payment Section -->
        <div class="card bg-danger text-white mt-4">
            <div class="card-body">
                <h4 class="card-title">Group: <%=group%></h4>
                <p class="card-text">Members: <%=members%></p>
            <form id = "addPayment" method = "POST" action="/split/group">
                <input type ="hidden" name ="group" value ="<%=group%>">
                <div class="d-grid gap-2">
                    <button class="btn btn-light text-danger fw-bold">Add a payment</button>
                </div>
                <p class="mt-3 text-black-50 small">
                    Register payment records from the "Add a payment" button
                </p>
            </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
